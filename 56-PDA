import plotly.plotly as py
import plotly.graph_objs as go
py.sign_in('pasit_st', '6useml50b5')

fig = {'data': [{'labels': ['Bangkok', 'Central region', 'Northern region', 'Northeastern region', 'Southern region'], 'values': [83400, 60391, 45635, 62526, 51065], 'type': 'pie'}], \
       'layout': {'title': 'PDA 2556'}}

url = py.plot(fig, filename='56 PDA')
